info:
  name: POST juegos
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/juegos
  body:
    type: json
    data: |-
      {
        "titulo": "Elden Ring",
        "genero": "RPG",
        "precio": 39990
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
