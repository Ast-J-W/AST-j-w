info:
  name: POST resenas error campos
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/resenas
  body:
    type: json
    data: |-
      {
        "juego_id": 1,
        "autor": "Jesus"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
