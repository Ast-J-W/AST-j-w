info:
  name: POST resenas error juego
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/resenas
  body:
    type: json
    data: |-
      {
        "juego_id": 999,
        "autor": "Jesus",
        "calificacion": 5,
        "comentario": "No existe ese juego"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
