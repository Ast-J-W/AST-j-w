info:
  name: GET  - Listar juegos
  type: http

http:
  method: GET
  url: http://localhost:3000/juegos
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
