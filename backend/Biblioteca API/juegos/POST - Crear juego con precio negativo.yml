info:
  name: POST - Crear juego con precio negativo
  type: http

http:
  method: POST
  url: http://localhost:3000/juegos
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
