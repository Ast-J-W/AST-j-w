info:
  name: POST - Crear resena sin autor
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/resenas
  body:
    type: json
    data: |-
      {
        "juego_id": 1,
        "calificacion": 5,
        "comentario": "Muy buen juego"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
