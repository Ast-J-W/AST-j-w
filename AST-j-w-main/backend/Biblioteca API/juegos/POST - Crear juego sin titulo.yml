info:
  name: POST - Crear juego sin titulo
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/juegos
  body:
    type: json
    data: |-
      {
        "genero": "Plataforma",
        "precio": 59990
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
