info:
  name: POST - Crear juego válido
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/juegos
  body:
    type: json
    data: |-
      {
        "titulo": "Dark Souls: Remastered",
        "genero": "RPG",
        "precio": 25499
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
