info:
  name: POST - Crear resena sin juego_id
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/resenas
  body:
    type: json
    data: |-
      {
        "autor": "Jesus",
        "calificacion": 5,
        "comentario": "Muy buen juego"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
